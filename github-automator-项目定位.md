# github-automator 项目定位

> 状态：2026-08-23 由用户与 AI 搭档共同拍板确定。
> 用途：本文件是项目方向的"锚点"，所有功能取舍、文档、skill 设计都以它为基准。
> 配套：技术细节见《项目介绍》《规范手册》；变更记录见《优化与更新记录》。

---

## 一、一句话定位

**github-automator 是「AI 助手的项目发布安全带」：一个零依赖的发布引擎，配一个 skill 让 AI 在任何对话里接管"打包上传 GitHub"全流程，并在发布前做隐私/体积闸门。用户只给路径（或说一句"发到 GitHub"），AI 完成其余一切，且不误传不该传的东西。**

---

## 二、我们为谁做（目标用户）

| 维度 | 明确范围 |
|------|----------|
| 主要用户 | **用户本人 + 其 AI 搭档（WorkBuddy / 类似 Agent）**——不是给陌生开发者用的开源产品 |
| 使用场景 | 用户在任何项目对话里说"帮我发到 GitHub" / 给一个路径，AI 加载 skill 后接管发布 |
| 不做 | 不做面向公网的分发型开源工具（不跟 `gh` CLI 硬刚、不追求 star 数） |

**含义**：工具是"引擎"，skill 是"AI 的操作规范 + 前置审查"。价值不在"能不能传"，而在"AI 帮用户**安全地、不用动脑地**传"。

---

## 三、解决什么（核心价值 & 差异化）

`gh` CLI 本身能完成发布——所以本工具**没有"不可替代性"**，它的价值在三个"人/裸脚本最容易翻车"的点，已被真实发布踩坑验证：

1. **防误传**：自动排除 `.venv/`(210M)、`__pycache__/`、密钥点文件、自定义 `.gitignore` 命中的目录（如 `logs/` 421M、`outputs/` 305M 游戏截图）。手动发极易漏、极易传超大文件。
2. **流程固化且可复现**：分析→生成 .gitignore→生成 README→打包 zip 快照→建仓→推送→打 Release，每次都按同一套做对，不留"凭记忆漏一步"的空间。
3. **边界容错**：行内注释解析、嵌套 .gitignore 不泄漏、Release 创建幂等（撞已存在 tag 自动续传资产而非崩）。这些正是手工流程的高频事故点。

**差异化一句话**：别人做"怎么传"，我们做"怎么**安全地、不用脑地**传"。

---

## 四、明确不做（边界，永久或半永久）

为避免范围蔓延，以下明确排除：

- ❌ 不做重型错误处理框架 / 插件化 / 引入配置文件（保持零依赖、单文件可懂）。
- ❌ 不做面向公网的开源运营（文档站、社区、star 指标）。
- ❌ 不追求跨生态全覆盖的 `.gitignore` 生成（覆盖 90% 常见场景即可）。
- ❌ 不做语义翻译级仓名（自动转写仅到"音节级拼音"；语义英文名由用户/AI 经 `--repo` 指定）。
- ⏸ 待议（非永久不做）：多平台 Release（GitLab 等）、已发布检测/重复运行幂等 UI、依赖解析展示增强。

---

## 五、产品形态（引擎 + skill 双层）

```
┌─────────────────────────────────────────────┐
│  Skill（AI 操作规范 + 前置审查）              │
│  - 识别"发布意图" → 调引擎                     │
│  - 发布前扫隐私/大文件，建议排除项             │
│  - 帮用户定语义化 --repo / 版本策略           │
│  - 零交互模式：用户说"直接发"则跳过审查       │
└──────────────────┬──────────────────────────┘
                   │ 调用
┌──────────────────▼──────────────────────────┐
│  引擎 github-automator（CLI，零依赖）         │
│  analyze → gitignore → README → zip           │
│  → create_repo → push → create_release        │
│  零交互、幂等、token 不落盘                    │
└─────────────────────────────────────────────┘
```

- **引擎**已落地（v0.1.0 基础 + Phase 1–5 修复，46 测试全绿）。
- **skill** 为下一步：把"AI 怎么正确用引擎、发布前注意什么"固化，使换工作区/换对话都能复用。

---

## 六、成功标准（怎么算做成了）

- [x] 引擎：零依赖、零交互、自举发布、46 测试全绿、真实项目验证过（发了 `sevenday-automation`）。
- [ ] skill：AI 加载后能在任意项目对话里一句话接管发布，且发布前能拦住隐私/大文件。
- [ ] 用户体感："说一句就能发，且从没误传过错的东西"。

---

## 七、下一步（按优先级）

1. **做 skill**（最高优先）：固化"AI 辅助发布"流程，含隐私闸门 + 语义仓名建议 + 零交互降级。
2. 引擎小修补：极生僻/繁体字仓名回退优化（可选）。
3. 文档对齐：本定位文档纳入 README 导航与规范手册"发展方向"节。

---

## 八、变更记录

- 2026-08-23：用户与 AI 共同确认定位为 **B（AI 助手的项目发布安全带）**，明确"用户=本人+AI、不做公网开源产品"，定稿本文件。
